A.List the workflow og using Git.


create a folder ,highlights the path and type cmd to direct it on cmd.exe
mkdir
cd documents
cd scheduling_balmes 
git init = initialized git repository
git config = to config AdrianBalmes or Adrianbalmes211@gmail.com
git add . = to add all files
git commit -m "main"git 
git branch 
git remote add origin https://github.com/AdrianBalmes/itp19_balmes.git
git push -u origin main = push files.


share the best practice in your git add,commit
answer:The best practice in `git add` and `git commit` is to add only important changes, use clear commit messages, and save updates regularly to keep the project organized.
